const express = require('express'); const cors = require('cors'); const helmet = require('helmet'); require('dotenv').config(); const app = express(); app.use(helmet()); app.use(cors()); app.use(express.json()); app.get('/health', (req, res) => res.json({ status: 'healthy', service: 'tournament-service' })); const PORT = process.env.PORT || 3004; app.listen(PORT, () => console.log('Tournament service running on port ' + PORT));
