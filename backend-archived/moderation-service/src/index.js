const express = require("express"); const cors = require("cors"); const helmet = require("helmet"); require("dotenv").config(); const app = express(); app.use(helmet()); app.use(cors()); app.use(express.json()); app.get("/health", (req, res) => res.json({ status: "healthy", service: "moderation-service" })); const PORT = process.env.PORT || 3005; app.listen(PORT, () => console.log(`Moderation service running on port ${PORT}`));
