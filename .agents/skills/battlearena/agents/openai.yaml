interface:
  display_name: "Battlearena"
  short_description: "Repo-specific patterns and workflows for battlearena"
  default_prompt: "Use the battlearena repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true