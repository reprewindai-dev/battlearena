# Supabase

This folder contains SQL migrations intended for Supabase Postgres.

- Apply migrations using the Supabase CLI, or paste the migration SQL into the Supabase SQL editor for early prototyping.
- `migrations/0001_arena_foundation.sql` contains schema + RLS + RPC stubs.
